<h1 style="color: black">Archived<i>tasks</i></h1>
<div class="container row">
<table class="table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th>Last worked on</th>
      <th>Deadline</th>
      <th>Date completed</th>
      <th>Notes</th>
      <th>Links</th>
      <th>Category</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @tasks.order(:deadline).each do |task| %>
      <% if task.completed == true %>
      <tr>
        <td><%= task.name %></td>
        <td><%= task.last_worked_on.strftime("%b/%-d") %></td>
        <td><p style="color: green"><%= task.deadline.strftime("%m/%d/%y @ %H:%M") %></p></td>
        <td><b><%= task.date_completed.strftime("%b %-d, %Y") %></b></td>
        <td><p style="color: black"><%= task.notes.chars[0..20].join %></p>
        <% if task.notes.length > 20 %>
          <u><a href="/tasks/notes/<%= task.id %>" style="color: blue">more...</a></u></td>
        <% end %>
        <td><%= task.links.chars[0..10].join %></td>
        <td><p style="color: #550808"><%= task.category %></p></td>
        <td><%= link_to 'Show', task, {:style=>'color:white;', :class => "btn btn-success"} %></td>
        <td><%= link_to 'Edit', edit_task_path(task), {:style=>'color:white;', :class => "btn btn-info"} %></td>
        <td><%= link_to 'Destroy', task, method: :delete, data: { confirm: 'Are you sure?' }, :class => 'btn btn-primary', :style => 'color: white;'%></td>
      </tr>
    <% end %>
    <% end %>
  </tbody>
</table>
</div>

<%= link_to 'Back', tasks_path, {:style=>'color:white;', :class => "btn btn-primary"} %>

Notes: Archive tasks that might for reference of accomplishments, possible resume / interview examples  or things to make note of before deletion.
