<!--
in controller...

def practice2
    require 'open-uri'
    require 'json'
      url = URI.parse("http://ats-rails-project.herokuapp.com")
      open(url) do |http|
      @response = http.read
      @response = @response[0]
      end
    render :practice2, layout: false and return
  end
--!>


<%= @response %>